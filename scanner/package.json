{
  "name": "article50-scanner",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Detects EU AI Act Article 50 transparency exposure from public web surfaces.",
  "bin": { "scan": "./cli.js" },
  "scripts": {
    "scan": "node cli.js",
    "test": "node --test --test-force-exit test/*.test.js"
  },
  "dependencies": {
    "playwright": "^1.56.1"
  }
}
